import React from 'react';
import Carousel from '@/app/animation/Carousel';
import Image from 'next/image';

const Main = () => {
  return( 
    <section id="home">
      <Carousel/>
     
    </section>
  );
};

export default Main;
